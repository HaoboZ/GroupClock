// @flow

import variable from "./../variables/platform";

export default (variables /*: * */ = variable) => {
	const bodyTheme = {
		flex:       3,
		alignItems: "center",
		alignSelf:  "center"
	};
	
	return bodyTheme;
};
